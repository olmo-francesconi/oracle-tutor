# TODO: Oracle Tutor

## Current focus
Semantic-only API migration in progress on `semantic-api` branch — removing fuzzy fallback, unifying the search surface around pgvector.

## Up next
- [ ] Add `.env.example` for local dev onboarding
- [ ] Apply server-side filtering in semantic endpoints (color identity, CMC, type, format legality)
- [ ] Confirm Railway Cron schedule and worker token rotation process
- [ ] Review and flesh out ROADMAP milestones with the team

## Blocked
<!-- Anything waiting on external input -->
