# frontend/.env.example
# Copy to frontend/.env and fill in. Loaded by docker compose into the frontend
# container via env_file. Most values are forced inline by docker-compose.yml;
# this file is only for overrides.

# VITE_API_URL=/api
# VITE_APP_URL=https://oracletutor.org
